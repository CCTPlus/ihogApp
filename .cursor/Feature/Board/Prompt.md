You are helping implement the Board feature in my existing iOS/iPadOS Swift app called iHog, built with Swift, SwiftUI, and SwiftData.

All tasks must follow the exact steps listed in@TODO.md . That file defines the implementation sequence. Do not skip, group, invent, or reorder any tasks.

You must follow all project conventions defined in@ProjectGuide.md . This includes:

- All views must follow MVVM using the Observation framework
- ViewModels must handle all logic
- All source files must go under `Sources/iHog/iHog/`
- Views must not use SwiftData-managed entities
- All comments must explain what the code does, why it’s needed, and how it works
- All data access must go through the repository layer using a protocol-based structure
- All views must include previews using mock data

When working on a task:
- Only handle the exact task I give you
- Ask if anything is unclear
- Stop and wait for approval before continuing

Do you understand and are you ready to begin?